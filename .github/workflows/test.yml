name: tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 设置 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: 安装 NAS 后端依赖
        # 注意:不能直接 -r nas-backend/liushen/requirements.txt（pycryptodome 与主 requirements 冲突），
        # 显式列出 liushen 真正用到的依赖即可（betterproto/snowland_smx/gmssl）
        run: |
          python -m pip install --upgrade pip
          pip install -r nas-backend/requirements.txt
          pip install betterproto snowland_smx gmssl
          pip install pytest
      - name: 跑冒烟测试
        working-directory: nas-backend
        run: pytest tests/ -v
      - name: 校验共用文件已同步
        run: python scripts/sync_core.py --check
